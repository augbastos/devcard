# Lint for the stdlib-only Python side (hook/, scripts/, install.py). Ruff's
# default correctness rules plus bugbear: real defects, not style. Formatting
# is not enforced.
target-version = "py311"

[lint]
select = ["E4", "E7", "E9", "F", "B"]
