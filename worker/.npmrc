# Dependency install scripts do not run. The toolchain needs none of them —
# esbuild and workerd resolve their platform binaries through optional
# dependencies — and a compromised transitive package's `postinstall` is the
# classic npm supply-chain entry point. `npm test` and `npm run` are unaffected.
ignore-scripts=true
engine-strict=true
