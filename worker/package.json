{
  "name": "devcard-worker",
  "version": "1.0.0",
  "description": "devcard worker — renders the live SVG card from D1 rollups",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit && tsc --noEmit -p test/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260901.1",
    "typescript": "^5.5.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  }
}
